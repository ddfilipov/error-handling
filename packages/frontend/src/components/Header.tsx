"use client";
import { FC } from "react";

const Header: FC = ({}) => {
    return (
        <div>
            <h1 style={{ textAlign: "center" }}>Error handling 101</h1>
        </div>
    );
};

export default Header;
