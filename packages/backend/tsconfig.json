{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "moduleResolution": "node",
        "outDir": "./dist",
        "baseUrl": ".",
        "paths": {
            "@common/*": ["../../common/*"]
        }
    },
    "include": [
        "src/**/*",
        "../data/**/*"  // Assuming you want to include this directory as well
    ],
    "exclude": ["node_modules"]
}
